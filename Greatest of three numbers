Write a program to find which number is greatest among three numbers.

#include<iostream>
#include<cmath>
using namespace std;

int main()
{
    int a,b,c;
    cin >> a >> b >> c;
    if(a>b && a>c)
    {
        cout << a << "\t" << "is greater";
    }
    else if(b>a && b>c)
    { cout << b << "\t" << "is greater";
    }
    else
    {
        cout << c << "\t" << "is greater";
    }
    return 0;
}
